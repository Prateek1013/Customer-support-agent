export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {}, // Autoprefixer is optional in v4 but good to keep if needed, though sometimes included. Let's keep it safe. Actually v4 includes it.
  },
}
